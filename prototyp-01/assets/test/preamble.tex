\KOMAoption{fontsize}{16pt}
\newcommand{\myPageWidth}{8cm}
\newcommand{\setSmallFont}{\KOMAoption{fontsize}{12pt}}

\usepackage[margin=0.5cm, paperwidth=\myPageWidth, paperheight=10in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[ngerman]{babel}
\usepackage[active, tightpage]{preview}
\renewcommand{\PreviewBorder}{5pt} % Adds a little padding around the crop
\usepackage{csquotes}
\usepackage[default]{sourcesanspro}
\usepackage{scrlayer-scrpage}
\usepackage{eulervm} % coole Mathe-Schriftart
\usepackage[most]{tcolorbox}
\usepackage[dvipsnames]{xcolor}
\usepackage{tabularx}
\usepackage{mathtools}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{MnSymbol}
\usepackage{enumitem}
\usepackage{stmaryrd}
\usepackage{bm}
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{graphicx}
\pgfplotsset{compat=1.18}

% sans-serif Schriftart
\renewcommand{\familydefault}{\sfdefault} 
\setlength{\parindent}{0pt}

% Mathe-Macros
\newcommand{\N}{\mathbb{N}}
\newcommand{\R}{\mathbb{R}}

% Farben
\definecolor{akademusBlue}{HTML}{264358}
\colorlet{myBlue}{RoyalBlue}
\colorlet{myOrange}{Orange}

% This redefines the 'default' color at the low level
\makeatletter
\newcommand{\globalcolor}[1]{%
  \color{#1}\def\default@color{\current@color}%
}
\makeatother

% Apply it globally
\AtBeginDocument{\globalcolor{akademusBlue}}

% Tikz-Einstellungen
\tikzset{
    every picture/.append style={
        line width=1.5pt,
        line cap=round,
        line join=round
    }
}

\pgfplotsset{
    % Standard Behavior for all Axes
    every axis/.append style={
        % FIX: Move trim keys here so they only look for the axis once it exists
        trim axis left,
        trim axis right,
        % -----------------------------------------------------------------
        % width=\linewidth,
        axis lines = middle,
        axis line style={line width=1pt},
        tick style={line width=0.1pt},
        xlabel = $x$,
        ylabel = {$y$},
        ymin=-10, ymax=10,
        xmin=-3, xmax=7,
        xtick distance=1,
        ytick distance=1,
        grid=both,
        grid style={line width=.1pt, draw=gray!20},
        xticklabels={},
        yticklabels={},
        restrict y to domain=-15:15,
    },
    % Standard Behavior for all Plot Lines
    every axis plot/.append style={
        line width=2.5pt,
        samples=100
    }
}


\tcbset{
  colupper=akademusBlue, % Standardfarbe in tcolorbox
  enhanced,
  frame hidden,
  boxrule=0pt, 
  colback=gray!5!white,  
  rounded corners,
}


% Boxes
\newtcolorbox{marked}{
  borderline west={3pt}{0pt}{myOrange}, 
  valign=center,       
}

\newtcolorbox{example}{  
  enhanced,
  frame hidden,
  before upper={\textcolor{myBlue}{\bfseries Beispiel:}},       
  colback=gray!5!white,       
  borderline west={3pt}{0pt}{myBlue}, 
  boxrule=0pt,         
  sharp corners,
}

\pagestyle{empty}
